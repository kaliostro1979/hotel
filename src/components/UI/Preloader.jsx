import PreloaderIcon from "../../icons/PreloaderIcon";

const Preloader = () => {
    return (
        <div className={"Preloader"}>
            <PreloaderIcon/>
        </div>
    );
};

export default Preloader
